*** Settings ***
Library		Selenium2Library	
Library		String
Library		Collections	
Resource	../Common/Common_Variables.txt
Resource	../Resources/Keywords.txt	
Suite Setup		Open Intranet and Login
Suite Teardown	Logout And Close Browser


*** Test Cases ***
Open Phonebook And Verify All Columns According to Ascending and Descending
	Wait Until Element Is Visible		${HomeButton}
	Mouse Over		${HomeButton}
	Wait Until Element Is Visible		${PhonebookButton}
	Click Element	${PhonebookButton}
	Wait Until Element Is Visible		${TableRows}
	Log			${ascendingLetters}
	Log			${letters}
	
	
Verify Name Column According to Ascending and Descending
	Verify Column According to Order			${NameAscendingButtonImage}			${AscendingLetters}		${NameTableCell}
	Verify Column According to Order			${NameDescendingButtonImage}		${letters}				${NameTableCell}


Verify RA Name Column According to Ascending and Descending	
	Verify Column According to Order			${RANameAscendingButtonImage}		${AscendingLetters}		${RANameTableCell}	
	Verify Column According to Order			${RANameDescendingButtonImage}		${letters}				${RANameTableCell}

Verify Designation Column According to Ascending and Descending	
	Verify Column According to Order			${DesignationAscendingButtonImage}		${AscendingLetters}		${DesignationTableCell}
	Verify Column According to Order			${DesignationDescendingButtonImage}		${letters}				${DesignationTableCell}
	
Verify Department Column According to Ascending and Descending	
	Verify Column According to Order			${DepartmentAscendingButtonImage}		${AscendingLetters}		${DepartmentTableCell}
	Verify Column According to Order			${DepartmentDescendingButtonImage}		${letters}				${DepartmentTableCell}

Verify EmailId Column According to Ascending and Descending	
	Verify Column According to Order			${EmailIdAscendingButtonImage}		${AscendingLetters}			${EmailIdTableCell}
	Verify Column According to Order			${EmailIdDescendingButtonImage}		${letters}					${EmailIdTableCell}

Verify Locaton Column According to Ascending and Descending	
	Verify Column According to Order			${LocatonAscendingButtonImage}		${AscendingLetters}			${LocationTableCell}
	Verify Column According to Order			${LocatonDescendingButtonImage}		${letters}					${LocationTableCell}

Verify Skills Column According to Ascending and Descending	
	Verify Column According to Order			${SkillsAscendingButtonImage}		${AscendingLetters}			${SkillsTableCell}
	Verify Column According to Order			${SkillsDescendingButtonImage}		${letters}					${SkillsTableCell}

Verify Experience Column According to Ascending and Descending
	Verify Column According to Order			${ExperienceAscendingButtonImage}		${AscendingLetters}			${ExperienceTableCell}
	Verify Column According to Order			${ExperienceDescendingButtonImage}		${letters}					${ExperienceTableCell}
	
Verify PhoneNumber Column According to Ascending and Descending
	Verify Column According to Order			${PhoneNumberAscendingButtonImage}		${AscendingLetters}			${PhoneNumberTableCell}
	Verify Column According to Order			${PhoneNumberDescendingButtonImage}		${letters}					${PhoneNumberTableCell}

Verify ExtNo Column According to Ascending and Descending
	Verify Column According to Order			${ExtNoAscendingButtonImage}		${AscendingLetters}			${ExtNoTableCell}
	Verify Column According to Order			${ExtNoDescendingButtonImage}		${letters}					${ExtNoTableCell}
